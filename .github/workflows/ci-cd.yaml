name: ci-cd developments / Pipeline

on: [push]

jobs:
   build:
     name: 🛠️ Build Frontend
     runs-on: ubuntu-latest
     steps:
        name: checkout
        uses: actions/checkout@v3
        run: npm i